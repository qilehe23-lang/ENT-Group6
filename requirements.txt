# requirements.txt
# Deadline Survivor — Python 依赖
# 安装命令：pip install -r requirements.txt

# ── OS 交互 ──────────────────────────────────────────────────────────────────
keyboard==0.13.5        # 全局热键监听 + 键盘事件模拟
pyperclip==1.8.2        # 跨平台剪贴板读写

# ── LLM 集成（Phase 2）───────────────────────────────────────────────────────
groq==0.9.0             # Groq 官方 Python 客户端（Llama-3 推理）

# ── GUI / 系统托盘（Phase 3）─────────────────────────────────────────────────
PyQt5==5.15.10          # 系统托盘、设置对话框

# ── 打包（Phase 5）───────────────────────────────────────────────────────────
pyinstaller==6.6.0      # 编译为 .exe / .app
